<h1>This is the homepage of the blogs!</h1>

<div class='row text-center' style='display:flex; flex-wrap:wrap;'>
        <% blogs.forEach(function(blog){ %>
            <div class='col-md-3 col-sm-6'>
                <div class='thumbnail'>
                    <div class='caption'>
                        <h2><%= blog.title %></h2>
                    </div>
                    <img src="<%= blog.image %>">
                    <img src="<%= blog.image %>">
                    <span><%= blog.create %></span>
                    <p><%= blog.body %></p>
                </div>
            </div>
        <% }); %>
</div>